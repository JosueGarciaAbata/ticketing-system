info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/v1/events/"
  body:
    type: json
    data: |-
      {
        "title": "Evento 1",
        "description": "Descripcion del evento",
        "category": "Concierto",
        "durationMinutes": 130
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
