info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{host}}/api/v1/events/2"
  body:
    type: json
    data: |-
      {
        "title": "Evento testing",
        "description": "Descripcion del evento",
        "category": "Concierto",
        "durationMinutes": 130
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
