info:
  name: findAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/seats/shows/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
