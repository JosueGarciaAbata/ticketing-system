info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{host}}/api/v1/cities/3"
  body:
    type: json
    data: |-
      {
        "name": "Tena",
        "country": "Ecuador",
        "timezone": "America/Guayaquil"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
