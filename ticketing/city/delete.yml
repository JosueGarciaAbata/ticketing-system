info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/api/v1/cities/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
