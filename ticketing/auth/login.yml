info:
  name: login
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/v1/login
  body:
    type: json
    data: |-
      {
        "email": "josuegarcia@admin.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
