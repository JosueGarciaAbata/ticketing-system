info:
  name: login
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/auth/login
  body:
    type: json
    data: |-
      {
        "email": "client@test.com",
        "password": "password123"
      }x
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
