info:
  name: register
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/users/reg/organizer
  body:
    type: json
    data: |-
      {
        "email": "davidm@test.com",
        "password": "password123",
        "firstName": "David",
        "lastName": "Manjarres"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
