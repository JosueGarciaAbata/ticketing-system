info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/v1/shows/"
  body:
    type: json
    data: |-
      {
        "eventId": 2,
        "venueId": 2,
        "capacity": 20,
        "startTime": "2026-02-11T05:05:00.000Z",
        "endTime": "2026-02-11T05:05:00.000Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
