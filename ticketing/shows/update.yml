info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{host}}/api/v1/venues/1"
  body:
    type: json
    data: |-
      {
        "cityId": 1,
        "name": "Terminal del norte",
        "capacity": 60
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
